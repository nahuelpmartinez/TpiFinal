#include <iostream>
#include "Menu.h"
#include <locale>
#include <windows.h>

using namespace std;

int main(){
    //setlocale(LC_ALL, "es_ES.UTF-8");
    //SetConsoleOutputCP(CP_UTF8); // Configura la salida de consola a UTF-8
    //SetConsoleCP(CP_UTF8);       // Configura la entrada de consola a UTF-8
    MenuPrincipal();

    return 0;
}
